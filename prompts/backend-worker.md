# Backend Worker Prompt

Use `prds/prd.md` and `prds/prd.json` as the execution contract. Implement only the single-tenant LingoBridge MVP backend: demo auth, course/courseware upload, PDF/PPTX page fallback, Excel-to-exercise parsing, TTS facade, student recordings, teacher lecture replays, and health checks.

Do not implement multi-tenant SaaS, payments, social features, real-time subtitles, or AI pronunciation scoring. Do not hardcode or commit real secrets.

Own backend files only unless the task explicitly asks for cross-layer changes.

