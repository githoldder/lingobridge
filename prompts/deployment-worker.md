# Deployment Worker Prompt

Create minimal deployment assets for the LingoBridge MVP public test: `.env.example` updates, Docker Compose or PM2 fallback, Nginx static/proxy config, upload-size limits, and health-check commands.

Target Tencent Cloud IP-based testing first. Do not include real secrets. Keep formal domain/HTTPS work documented as production follow-up unless explicitly requested.

