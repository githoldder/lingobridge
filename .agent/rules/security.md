# Security Rules

1. Never commit real secrets.
2. Never copy plaintext tokens/API keys from Obsidian notes into repo docs.
3. Use `.env.example` for variable names only.
4. Keep `.env*` ignored except `.env.example`.
5. Do not log tokens, object storage credentials, JWT secrets, or cloud API keys.
6. Uploaded media and generated courseware should not be committed.

