@startuml defect-lifecycle-state

skinparam backgroundColor #FFFFFF
skinparam StateBackgroundColor #3B82F6
skinparam StateBorderColor #2563EB
skinparam StateFontColor #FFFFFF
skinparam ArrowColor #3B82F6
skinparam NoteBackgroundColor #EFF6FF
skinparam NoteBorderColor #3B82F6

title 缺陷生命周期状态图

[*] --> 新建 : 提交缺陷

state "新建\n(New)" as 新建 #3B82F6
state "已确认\n(Confirmed)" as 已确认 #3B82F6
state "修复中\n(In Progress)" as 修复中 #10B981
state "已修复\n(Fixed)" as 已修复 #10B981
state "验证中\n(Verifying)" as 验证中 #3B82F6
state "已关闭\n(Closed)" as 已关闭 #10B981
state "延期\n(Deferred)" as 延期 #F43F5E
state "重新打开\n(Reopened)" as 重新打开 #F43F5E
state "已拒绝\n(Rejected)" as 已拒绝 #F43F5E

新建 --> 已确认 : 确认为有效缺陷
新建 --> 已拒绝 : 非缺陷 / 无法复现

已确认 --> 修复中 : 分配开发者
已确认 --> 延期 : 优先级低 / 资源不足

修复中 --> 已修复 : 提交修复代码

已修复 --> 验证中 : 提交测试验证

验证中 --> 已关闭 : 验证通过
验证中 --> 重新打开 : 验证失败\n缺陷仍存在

重新打开 --> 修复中 : 重新分配修复

延期 --> 修复中 : 排入迭代计划
已拒绝 --> 已关闭 : 归档处理

已关闭 --> [*]

note right of 新建
  优先级标签:
  P0 - 阻塞 (Blocker)
  P1 - 严重 (Critical)
  P2 - 一般 (Major)
  P3 - 次要 (Minor)
end note

note right of 修复中
  关联分支:
  fix/<defect-id>
  修复后提交 PR
end note

note left of 延期
  延期原因需记录
  每迭代重新评审
end note

note left of 重新打开
  附带失败截图
  与修复 commit 对比
end note

@enduml
