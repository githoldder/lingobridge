\chapter{数据库详细设计}

\section{数据库与存储架构设计}
LingoBridge 系统采用了敏捷轻量化的本地文件持久化存储方案与关系型内存映射模型相结合的双重设计。在生产环境下，为了保证开发迭代的高效性与运行的自闭环特性，核心数据在底层被抽象为一个统一的键值结构的扁平化文件。系统在启动时，底层的存储引擎会自动读取该文件的内容，并在内存中构建完整的多表关系映射对象。每次客户端发起状态更新请求或者写入动作时，服务端的数据访问组件会首先对内存缓存中的数据进行原子性更新，并在其后立即触发文件落盘动作。该方案采用了单线程排他写锁机制，保证在并发写请求下数据的强一致性，有效规避了由于多线程争用所导致的文件内容损坏与脏读现象。

\section{实体关系设计}
为了保障系统的高性能以及后期向传统关系型数据库如关系型数据库的平滑迁移，内存对象的设计完全遵循第三范式的要求，对各实体之间的主外键级联关系、主键唯一约束以及复合索引进行了严密的定义。整个 LingoBridge 的领域实体关系如图\ref{fig:database-erd}所示，该图直观展示了从用户鉴权、角色权限、教师学生结对关系，到核心的课程、课件文件、直播课堂、多媒体录音以及智能语音测评分析实体之间的多维级联关系。

\begin{figure}[H]
\centering
\includegraphics[width=0.9\textwidth]{database-erd.pdf}
\caption{LingoBridge 数据库实体关系图}
\label{fig:database-erd}
\end{figure}

\section{数据库表结构详细规格}
在详细物理设计阶段，系统为这二十个核心实体表定义了严格的数据类型、长度范围、主外键约束以及字段描述。

表\ref{tab:db-users}定义了整个系统的用户基础账号信息，包含登录凭证与基本账户状态。

\begin{table}[H]
\centering
\caption{用户基本信息表 (users) 结构规格}
\label{tab:db-users}
\begin{tabularx}{\textwidth}{lllX}
\toprule
字段名称 & 数据类型 & 主外键属性 & 描述与业务约束 \\
\midrule
id & VARCHAR(36) & 主键 (PK) & 用户全局唯一标识，符合UUID第四版规范 \\
username & VARCHAR(50) & 唯一索引 (UK) & 用户登录账号，仅支持字母、数字与下划线 \\
password\_hash & VARCHAR(255) & - & 经过加盐加密存储的不可逆口令密码密文 \\
role\_id & VARCHAR(36) & 外键 (FK) & 关联系统角色表，用以决定基本身份属性 \\
email & VARCHAR(100) & 索引 (IDX) & 用户的电子邮箱地址，用于密码找回与通知 \\
phone & VARCHAR(20) & 唯一索引 (UK) & 用户的联系电话号码，主要用于短信登录验证 \\
status & VARCHAR(20) & - & 账户状态，可选正常、禁用或等待审核 \\
created\_at & TIMESTAMP & - & 用户注册成功的时间，系统自动生成 \\
updated\_at & TIMESTAMP & - & 账户资料最近被修改的时间，系统自动更新 \\
\bottomrule
\end{tabularx}
\end{table}

表\ref{tab:db-user-profiles}存储了用户基本信息之外的扩展详情，主要包括头像与偏好设置。

\begin{table}[H]
\centering
\caption{用户详情表 (user\_profiles) 结构规格}
\label{tab:db-user-profiles}
\begin{tabularx}{\textwidth}{lllX}
\toprule
字段名称 & 数据类型 & 主外键属性 & 描述与业务约束 \\
\midrule
id & VARCHAR(36) & 主键 (PK) & 用户扩展详情的全局唯一标识，UUID格式 \\
user\_id & VARCHAR(36) & 外键 (FK) & 关联用户基本信息表主键，具备唯一性约束 \\
nickname & VARCHAR(50) & - & 用户在平台上展示的个性化友好昵称 \\
avatar\_url & VARCHAR(255) & - & 存储于服务器的文件路径或外部图片URL地址 \\
gender & VARCHAR(10) & - & 用户性别，可选男、女或保密 \\
bio & TEXT & - & 个人简介，支持富文本字符存储 \\
timezone & VARCHAR(50) & - & 用户所处时区，用于直播课程时间智能转换 \\
language & VARCHAR(10) & - & 界面首选交互语言，如中文简体或英文 \\
\bottomrule
\end{tabularx}
\end{table}

表\ref{tab:db-sessions}记录了客户端在鉴权成功后的活跃会话状态，用于维护在线安全。

\begin{table}[H]
\centering
\caption{用户会话状态表 (sessions) 结构规格}
\label{tab:db-sessions}
\begin{tabularx}{\textwidth}{lllX}
\toprule
字段名称 & 数据类型 & 主外键属性 & 描述与业务约束 \\
\midrule
id & VARCHAR(36) & 主键 (PK) & 会话的唯一识别标识，对应JWT内含标识 \\
user\_id & VARCHAR(36) & 外键 (FK) & 关联登录用户主键，表明该会话的所有者 \\
token & TEXT & - & 传输至客户端的完整鉴权签名令牌 \\
ip\_address & VARCHAR(45) & - & 客户端登录时的互联网协议地址 \\
user\_agent & VARCHAR(255) & - & 客户端浏览器或终端设备特征描述符 \\
expires\_at & TIMESTAMP & 索引 (IDX) & 会话过期失效的绝对时间节点 \\
is\_revoked & BOOLEAN & - & 会话是否已被强制吊销或登出作废 \\
\bottomrule
\end{tabularx}
\end{table}

表\ref{tab:db-roles}定义了系统运行所依赖的角色类别，是访问控制的顶层实体。

\begin{table}[H]
\centering
\caption{系统角色定义表 (roles) 结构规格}
\label{tab:db-roles}
\begin{tabularx}{\textwidth}{lllX}
\toprule
字段名称 & 数据类型 & 主外键属性 & 描述与业务约束 \\
\midrule
id & VARCHAR(36) & 主键 (PK) & 角色唯一主键标识，UUID格式 \\
name & VARCHAR(50) & 唯一索引 (UK) & 角色名称，例如系统管理员、教师、学生 \\
code & VARCHAR(20) & 唯一索引 (UK) & 角色代码简写，用于在代码逻辑中硬编码判断 \\
description & VARCHAR(255) & - & 角色的业务职责和权限边界的文字描述 \\
\bottomrule
\end{tabularx}
\end{table}

表\ref{tab:db-permissions}细化了系统各API接口和视图组件所要求的权限许可。

\begin{table}[H]
\centering
\caption{系统权限定义表 (permissions) 结构规格}
\label{tab:db-permissions}
\begin{tabularx}{\textwidth}{lllX}
\toprule
字段名称 & 数据类型 & 主外键属性 & 描述与业务约束 \\
\midrule
id & VARCHAR(36) & 主键 (PK) & 权限项全局唯一主键，UUID格式 \\
name & VARCHAR(100) & - & 权限的直观友好名称，用以在后台分配展示 \\
code & VARCHAR(50) & 唯一索引 (UK) & 权限操作符，如课程创建、作业批改等 \\
module & VARCHAR(50) & 索引 (IDX) & 该权限所属的功能业务模块分类标签 \\
\bottomrule
\end{tabularx}
\end{table}

表\ref{tab:db-role-permissions}是多对多权限模型中的关联关系表，确定了角色的权限清单。

\begin{table}[H]
\centering
\caption{角色权限关联表 (role\_permissions) 结构规格}
\label{tab:db-role-permissions}
\begin{tabularx}{\textwidth}{lllX}
\toprule
字段名称 & 数据类型 & 主外键属性 & 描述与业务约束 \\
\midrule
id & VARCHAR(36) & 主键 (PK) & 关联关系全局唯一主键 \\
role\_id & VARCHAR(36) & 外键 (FK) & 关联角色表，级联删除，不可为空 \\
permission\_id & VARCHAR(36) & 外键 (FK) & 关联权限表，级联删除，不可为空 \\
\bottomrule
\end{tabularx}
\end{table}

表\ref{tab:db-teacher-profiles}保存了担任教师职责的用户专属教学背景与资质认证信息。

\begin{table}[H]
\centering
\caption{教师专属信息表 (teacher\_profiles) 结构规格}
\label{tab:db-teacher-profiles}
\begin{tabularx}{\textwidth}{lllX}
\toprule
字段名称 & 数据类型 & 主外键属性 & 描述与业务约束 \\
\midrule
id & VARCHAR(36) & 主键 (PK) & 教师资质表的唯一标识主键 \\
user\_id & VARCHAR(36) & 外键 (FK) & 关联核心用户表，要求用户角色属性为教师 \\
title & VARCHAR(50) & - & 教师的职称或荣誉头衔 \\
bio & TEXT & - & 教师的教学理念和资质经历长文本 \\
rating & DECIMAL(3,2) & - & 教师在平台上的综合教学评价平均得分 \\
enrolled\_students & INTEGER & - & 教师名下累计绑定的学生总数统计 \\
\bottomrule
\end{tabularx}
\end{table}

表\ref{tab:db-student-profiles}保存了学生的当前中文级别、学习目的等个性化特征。

\begin{table}[H]
\centering
\caption{学生专属信息表 (student\_profiles) 结构规格}
\label{tab:db-student-profiles}
\begin{tabularx}{\textwidth}{lllX}
\toprule
字段名称 & 数据类型 & 主外键属性 & 描述与业务约束 \\
\midrule
id & VARCHAR(36) & 主键 (PK) & 学生专属档案主键，UUID格式 \\
user\_id & VARCHAR(36) & 外键 (FK) & 关联核心用户表，要求用户角色属性为学生 \\
chinese\_level & VARCHAR(20) & - & 学生的中文水平等级，如HSK一级到六级 \\
learning\_goal & TEXT & - & 学生选择学习中文的核心目的与背景 \\
preferred\_teacher & VARCHAR(36) & 外键 (FK) & 关联首选偏好的教师，用于后续课程推荐 \\
\bottomrule
\end{tabularx}
\end{table}

表\ref{tab:db-courses}定义了由教师开辟并进行教学设计的系统核心课程大纲。

\begin{table}[H]
\centering
\caption{课程基本信息表 (courses) 结构规格}
\label{tab:db-courses}
\begin{tabularx}{\textwidth}{lllX}
\toprule
字段名称 & 数据类型 & 主外键属性 & 描述与业务约束 \\
\midrule
id & VARCHAR(36) & 主键 (PK) & 课程唯一的全局识别主键 \\
teacher\_id & VARCHAR(36) & 外键 (FK) & 关联创建并主讲该门课程的教师主键 \\
title & VARCHAR(100) & - & 课程对外发布的官方大标题，不可为空 \\
description & TEXT & - & 该门课程的大纲、课程简介与目标群体文字 \\
cover\_image & VARCHAR(255) & - & 课程封面大图的存储路径 \\
price & DECIMAL(10,2) & - & 课程的定价，若为免费课程则显示为零 \\
is\_published & BOOLEAN & 索引 (IDX) & 表明该课程当前是否对外公开上架 \\
created\_at & TIMESTAMP & - & 课程创建的时间戳 \\
\bottomrule
\end{tabularx}
\end{table}

表\ref{tab:db-lessons}在课程之下派生，属于课程内的具体教学课时与节点单元。

\begin{table}[H]
\centering
\caption{课时信息节点表 (lessons) 结构规格}
\label{tab:db-lessons}
\begin{tabularx}{\textwidth}{lllX}
\toprule
字段名称 & 数据类型 & 主外键属性 & 描述与业务约束 \\
\midrule
id & VARCHAR(36) & 主键 (PK) & 课时唯一的全局识别主键，UUID格式 \\
course\_id & VARCHAR(36) & 外键 (FK) & 关联该课时所属的父级课程，级联删除 \\
title & VARCHAR(100) & - & 本节课时的教学大纲名称 \\
sort\_order & INTEGER & - & 课时在课程章节大纲中的排序序号 \\
description & TEXT & - & 课时的重点生词、核心句型等提纲说明 \\
courseware\_id & VARCHAR(36) & 外键 (FK) & 关联该课时预选绑定的多媒体教学课件文件 \\
created\_at & TIMESTAMP & - & 课时实体节点的创建时间 \\
\bottomrule
\end{tabularx}
\end{table}

表\ref{tab:db-assignments}定义了每个课时授课完毕后学生需要完成的录音口语作业。

\begin{table}[H]
\centering
\caption{课时配套作业表 (assignments) 结构规格}
\label{tab:db-assignments}
\begin{tabularx}{\textwidth}{lllX}
\toprule
字段名称 & 数据类型 & 主外键属性 & 描述与业务约束 \\
\midrule
id & VARCHAR(36) & 主键 (PK) & 作业节点的唯一标识，与课时形成一对一映射 \\
lesson\_id & VARCHAR(36) & 外键 (FK) & 关联课时节点主键，要求不可重复以保证映射 \\
title & VARCHAR(100) & - & 作业口语测评的名称 \\
prompt\_text & TEXT & - & 要求学生跟读或回答的中文目标引导文本 \\
audio\_template\_url & VARCHAR(255) & - & 教师上传的标杆示范发音音频文件存储地址 \\
max\_points & INTEGER & - & 该项口语评测的满分上限数值 \\
due\_date & TIMESTAMP & - & 作业提交的截止过期日期节点 \\
\bottomrule
\end{tabularx}
\end{table}

表\ref{tab:db-assignment-submissions}保存了学生针对某一节课口语作业的具体提交历史。

\begin{table}[H]
\centering
\caption{学生作业提交记录表 (assignment\_submissions) 结构规格}
\label{tab:db-assignment-submissions}
\begin{tabularx}{\textwidth}{lllX}
\toprule
字段名称 & 数据类型 & 主外键属性 & 描述与业务约束 \\
\midrule
id & VARCHAR(36) & 主键 (PK) & 作业提交记录的全局唯一主键 \\
assignment\_id & VARCHAR(36) & 外键 (FK) & 关联对应的课时配套口语作业主键 \\
student\_id & VARCHAR(36) & 外键 (FK) & 关联提交该份作业的学生主键 \\
audio\_record\_id & VARCHAR(36) & 外键 (FK) & 关联录音文件表，保存该次提交的真实音频 \\
submitted\_at & TIMESTAMP & - & 学生确认提交作业的真实系统时间 \\
score & DECIMAL(5,2) & - & 教师或系统引擎针对该次提交所打出的最终评分 \\
teacher\_comment & TEXT & - & 教师给出的口语指导个性化长文本评语 \\
\bottomrule
\end{tabularx}
\end{table}

表\ref{tab:db-learning-records}实时跟踪学生在整个平台上对每个课时节点的学习进度与完成度。

\begin{table}[H]
\centering
\caption{学生课时学习进度记录表 (learning\_records) 结构规格}
\label{tab:db-learning-records}
\begin{tabularx}{\textwidth}{lllX}
\toprule
字段名称 & 数据类型 & 主外键属性 & 描述与业务约束 \\
\midrule
id & VARCHAR(36) & 主键 (PK) & 学习记录的唯一识别主键 \\
student\_id & VARCHAR(36) & 外键 (FK) & 关联进行中文学习的学生唯一主键 \\
lesson\_id & VARCHAR(36) & 外键 (FK) & 关联正在学习或已完成的课时节点 \\
status & VARCHAR(20) & - & 学习状态，可选进行中或已圆满完成 \\
last\_accessed\_at & TIMESTAMP & - & 学生最后一次进入该课时学习界面的时间 \\
completed\_at & TIMESTAMP & - & 学生达成全部学习目标完成课时的时刻 \\
\bottomrule
\end{tabularx}
\end{table}

表\ref{tab:db-live-sessions}保存了教师发起的每一个实时互动直播课堂会话详情。

\begin{table}[H]
\centering
\caption{直播课堂会话实例表 (live\_sessions) 结构规格}
\label{tab:db-live-sessions}
\begin{tabularx}{\textwidth}{lllX}
\toprule
字段名称 & 数据类型 & 主外键属性 & 描述与业务约束 \\
\midrule
id & VARCHAR(36) & 主键 (PK) & 直播课堂实例唯一标识，作为WebSocket房间号 \\
lesson\_id & VARCHAR(36) & 外键 (FK) & 关联该次直播正在讲解的当前课时节点 \\
host\_teacher\_id & VARCHAR(36) & 外键 (FK) & 关联创建并主控此间课堂的授课教师 \\
status & VARCHAR(20) & 索引 (IDX) & 直播状态，可选准备中、进行中或已正常结课 \\
current\_page & INTEGER & - & 教师当前正演示同步的课件当前页码 \\
active\_students & INTEGER & - & 房间内当前正实时在线的学生数量 \\
started\_at & TIMESTAMP & - & 教师确认开始上课的系统绝对时刻 \\
ended\_at & TIMESTAMP & - & 直播课程圆满结束并断开连接的时刻 \\
\bottomrule
\end{tabularx}
\end{table}

表\ref{tab:db-live-chat-messages}保存了直播课堂中师生群聊发送的互动文本信息。

\begin{table}[H]
\centering
\caption{直播聊天记录表 (live\_chat\_messages) 结构规格}
\label{tab:db-live-chat-messages}
\begin{tabularx}{\textwidth}{lllX}
\toprule
字段名称 & 数据类型 & 主外键属性 & 描述与业务约束 \\
\midrule
id & VARCHAR(36) & 主键 (PK) & 单条聊天消息的唯一数据库标识主键 \\
live\_session\_id & VARCHAR(36) & 外键 (FK) & 关联当前交互所处的直播课堂会话主键 \\
sender\_id & VARCHAR(36) & 外键 (FK) & 关联发送该条消息的平台注册用户主键 \\
message\_text & TEXT & - & 聊天文本内容，支持长文字存储 \\
sent\_at & TIMESTAMP & - & 消息发出并被服务器转发的时刻 \\
\bottomrule
\end{tabularx}
\end{table}

表\ref{tab:db-live-whiteboard-strokes}记录了电子白板在直播过程中产生的墨迹坐标数据。

\begin{table}[H]
\centering
\caption{直播电子白板笔迹同步表 (live\_whiteboard\_strokes) 结构规格}
\label{tab:db-live-whiteboard-strokes}
\begin{tabularx}{\textwidth}{lllX}
\toprule
字段名称 & 数据类型 & 主外键属性 & 描述与业务约束 \\
\midrule
id & VARCHAR(36) & 主键 (PK) & 笔迹片段唯一标识 \\
live\_session\_id & VARCHAR(36) & 外键 (FK) & 关联对应的活跃直播课堂会话主键 \\
page\_number & INTEGER & 索引 (IDX) & 笔迹所描绘的当前课件对应具体页码 \\
drawer\_id & VARCHAR(36) & 外键 (FK) & 关联当前描线墨迹的绘制者用户主键 \\
stroke\_data & MEDIUMTEXT & - & 涂鸦笔画路径坐标、粗细与颜色的序列化JSON \\
created\_at & TIMESTAMP & - & 笔迹生成并落盘持久化的时间戳 \\
\bottomrule
\end{tabularx}
\end{table}

表\ref{tab:db-courseware-files}包含了教学过程中上传的各种实体演示文稿与辅助学习材料。

\begin{table}[H]
\centering
\caption{系统教学课件实体文件表 (courseware\_files) 结构规格}
\label{tab:db-courseware-files}
\begin{tabularx}{\textwidth}{lllX}
\toprule
字段名称 & 数据类型 & 主外键属性 & 描述与业务约束 \\
\midrule
id & VARCHAR(36) & 主键 (PK) & 课件文件的唯一主键，UUID格式 \\
uploader\_id & VARCHAR(36) & 外键 (FK) & 关联上传此课件的教师或管理员用户主键 \\
file\_name & VARCHAR(255) & - & 课件在文件系统中的存储友好名称 \\
file\_path & VARCHAR(255) & - & 课件保存在服务器或对象存储上的绝对路径 \\
file\_size & BIGINT & - & 文件的字节大小，用于在下载前显示 \\
mime\_type & VARCHAR(100) & - & 文件的媒体类型，如PDF、PPTX等格式 \\
total\_pages & INTEGER & - & 经后端解析后的文档总页数，用于同步控制 \\
uploaded\_at & TIMESTAMP & - & 文件上传成功并完成解析校验的时刻 \\
\bottomrule
\end{tabularx}
\end{table}

表\ref{tab:db-teacher-student-links}记录了教师和学生之间的强关联绑定结对状态。

\begin{table}[H]
\centering
\caption{师生结对关联映射表 (teacher\_student\_links) 结构规格}
\label{tab:db-teacher-student-links}
\begin{tabularx}{\textwidth}{lllX}
\toprule
字段名称 & 数据类型 & 主外键属性 & 描述与业务约束 \\
\midrule
id & VARCHAR(36) & 主键 (PK) & 映射记录的全局唯一主键，UUID格式 \\
teacher\_id & VARCHAR(36) & 外键 (FK) & 关联执行辅导和教学工作的教师主键 \\
student\_id & VARCHAR(36) & 外键 (FK) & 关联参与学习并接受指导的学生主键 \\
status & VARCHAR(20) & - & 结对关联状态，可选活跃或已终止关联 \\
linked\_at & TIMESTAMP & - & 双方绑定师生关系并生效的系统时间戳 \\
\bottomrule
\end{tabularx}
\end{table}

表\ref{tab:db-audio-recordings}记录了学生在口语发音测评中产生的多媒体音频源数据。

\begin{table}[H]
\centering
\caption{学生发音录音数据记录表 (audio\_recordings) 结构规格}
\label{tab:db-audio-recordings}
\begin{tabularx}{\textwidth}{lllX}
\toprule
字段名称 & 数据类型 & 主外键属性 & 描述与业务约束 \\
\midrule
id & VARCHAR(36) & 主键 (PK) & 录音文件全局唯一识别标识 \\
student\_id & VARCHAR(36) & 外键 (FK) & 关联生成该音频文件的学生主键 \\
file\_path & VARCHAR(255) & - & 音频文件在文件系统或对象存储上的绝对路径 \\
duration\_seconds & DECIMAL(6,2) & - & 录音音频的时长，以秒为单位，支持高精度 \\
format & VARCHAR(20) & - & 录音音频的压缩编码格式，如MP3或WAV \\
created\_at & TIMESTAMP & - & 录音生成并上传至系统完成存储的时刻 \\
\bottomrule
\end{tabularx}
\end{table}

表\ref{tab:db-pronunciation-evaluations}记录了由系统智能语音引擎打出的多维度发音打分详情。

\begin{table}[H]
\centering
\caption{智能发音测评与多维度打分表 (pronunciation\_evaluations) 结构规格}
\label{tab:db-pronunciation-evaluations}
\begin{tabularx}{\textwidth}{lllX}
\toprule
字段名称 & 数据类型 & 主外键属性 & 描述与业务约束 \\
\midrule
id & VARCHAR(36) & 主键 (PK) & 测评结果的全局唯一主键，UUID格式 \\
audio\_recording\_id & VARCHAR(36) & 外键 (FK) & 关联待进行特征提取的学生录音数据表 \\
completeness & DECIMAL(5,2) & - & 发音完整度指标得分，百分制计算 \\
accuracy & DECIMAL(5,2) & - & 声母、韵母发音精准度指标得分，百分制 \\
fluency & DECIMAL(5,2) & - & 口语表达流利度与节奏指标得分，百分制 \\
overall\_score & DECIMAL(5,2) & 索引 (IDX) & 系统引擎给出的综合加权汇总评分，百分制 \\
details\_json & LONGTEXT & - & 详细到音素、声调级的细粒度纠错分析数据 \\
\bottomrule
\end{tabularx}
\end{table}
